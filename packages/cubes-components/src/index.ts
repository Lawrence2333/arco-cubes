import { componentSchemaMap } from "./components/v2/schema";
import { componentMap } from "./components/v2";

const Components = {
  schema: componentSchemaMap,
  component: componentMap,
};

export default Components;
